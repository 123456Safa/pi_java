# PharmaX — Unified Database Configuration
# Used by: DatabaseConnection, MyDataBase, MyConnection (all pointing to same DB)
db.url=jdbc:mysql://localhost:3306/pharm?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&connectTimeout=3000&socketTimeout=5000&autoReconnect=true
db.user=root
db.password=
